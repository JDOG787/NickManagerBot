module.exports = {
  token: "", // this is the token. Make sure to keep it a secret!
  mongo: "", // this is the mongoDB url.

  prefix: "%", // this is the prefix, e.g. %help

  replit: false // if you are hosting on repl.it, turn this on.
}